// Founding member admin routes — STUB
// Authentication and authorisation are required before these routes can be
// activated. Implementation is deferred to Task #8 (admin dashboard).
// DO NOT mount this router without server-side auth middleware.

import { Router } from "express";

const router = Router();

export default router;
